name: "Flutter Test & Analyze"
description: "Réalise un flutter pub get, un analyze et des tests unitaires"
runs:
  using: "composite"
  steps:
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: 'stable'
    - run: flutter pub get
    - run: flutter analyze
    - run: flutter test
